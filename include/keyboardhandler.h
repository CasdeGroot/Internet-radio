/*	NAME: keyboardhandler.h
*	DESCRIPTION: Handles the key reading. Saves the states of the keys.
*	AUTHORS: group A1
*/

u_short get_pressed(void);
int	is_pressed(u_short);
u_short get_pressed_once(void);
